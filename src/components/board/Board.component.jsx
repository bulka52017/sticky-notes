import React from 'react';
import { BoardContainer, BorderWood } from './Board.styles';

const Board = () => {
    return (
        <BorderWood>
            <BoardContainer />
        </BorderWood>
    );
};

export default Board;
